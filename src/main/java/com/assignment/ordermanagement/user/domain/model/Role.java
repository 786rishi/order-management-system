package com.assignment.ordermanagement.user.domain.model;

/**
 * User Role enum - Domain model
 */
public enum Role {
    USER,
    PREMIUM_USER,  // Changed from PREMIUM_USER to PREMIUM for consistency
    ADMIN
}

